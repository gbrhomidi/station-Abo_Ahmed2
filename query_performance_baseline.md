# قياس أداء استعلامات التقارير والمبيعات

تم تنفيذ سلسلة من القياسات والتحسينات على استعلامات التقارير والمبيعات في طبقة `DatabaseHelper` للتأكد من كفاءتها وتوافقها مع متطلبات الأداء والعزل. يستعرض هذا التقرير حالة الاستعلامات قبل وبعد التحسينات، مع التركيز على خطط التنفيذ (EXPLAIN QUERY PLAN) واستهلاك الذاكرة.

## استعلامات صفحات المبيعات (getSalesPage)

كان الاستعلام السابق يعتمد على `SELECT * FROM sales_transactions`، مما يؤدي إلى جلب جميع الأعمدة من قاعدة البيانات بغض النظر عن حاجة الواجهة الأمامية، مما يزيد من استهلاك الذاكرة وحجم بيانات JSON المرسلة عبر الجسر.

تم تحسين الاستعلام ليستخدم إسقاطاً صريحاً (Explicit Projection) يقتصر على الحقول المطلوبة فقط: `s.id AS sale_id`، `s.sale_code`، `s.invoice_number`، و `COALESCE(p.commercial_name, '') AS customer_name`. 

| المقياس | قبل التحسين | بعد التحسين |
|---|---|---|
| الإسقاط (Projection) | جميع الأعمدة (`*`) | أعمدة محددة فقط |
| خطة التنفيذ الأساسية | `SEARCH s USING INDEX idx_sales_station_deleted` | `SEARCH s USING INDEX idx_sales_station_deleted` |
| الانضمام (JOIN) | `SEARCH p USING INTEGER PRIMARY KEY` | `SEARCH p USING INTEGER PRIMARY KEY` |
| الترتيب (ORDER BY) | `USE TEMP B-TREE` | `USE TEMP B-TREE` |

أظهرت خطة التنفيذ استخداماً فعالاً للفهرس المركب `idx_sales_station_deleted` لتصفية نطاق المحطة والسجلات غير المحذوفة. الانضمام مع جدول الأطراف يتم بكفاءة عالية (O(1)) عبر المفتاح الأساسي. الترتيب الزمني يتطلب شجرة B-Tree مؤقتة، وهو سلوك متوقع نظراً لعدم شمول الفهرس لحقل التاريخ بالترتيب المطلوب.

## تفاصيل الفاتورة (getInvoiceDetails)

تم تطبيق نفس منهجية الإسقاط الصريح على استعلام تفاصيل الفاتورة. بدلاً من جلب السجل بالكامل، يتم الآن تحديد الحقول الحيوية مثل `gross_amount` و `paid_amount` و `payment_method`.

تؤكد خطة التنفيذ استخدام الفهرس بكفاءة عالية، مع تطبيق `LIMIT 1` لضمان التوقف الفوري عند العثور على الفاتورة المطابقة. لا يتم استخدام `TEMP B-TREE` لعدم وجود شرط ترتيب.

## تحسين استعلامات العد (COUNT) للترقيم (Pagination)

أثناء مراجعة استعلامات الترقيم (Pagination) لصفحات المبيعات والطلبات والتوصيلات وتقارير المخزون، لوحظ أن استعلام `COUNT(*)` ينفذ انضماماً أيسر (`LEFT JOIN`) مع جداول مرتبطة (مثل `parties` أو `products`) في جميع الحالات.

تم إدخال منطق شرطي يحلل شروط التصفية (`WHERE`). إذا لم يقم المستخدم بالبحث عن نص يتطلب حقولاً من الجداول المرتبطة، يتم إسقاط الـ `LEFT JOIN` تماماً من استعلام العد.

| حالة البحث | خطة تنفيذ COUNT |
|---|---|
| بحث باسم العميل | `SEARCH s USING INDEX` + `SEARCH p USING INTEGER PRIMARY KEY LEFT-JOIN` |
| بدون بحث نصي | `SEARCH s USING INDEX idx_sales_station_deleted` فقط |

أدى هذا التحسين إلى التخلص من عمليات الانضمام غير الضرورية عند التنقل بين الصفحات، مما يقلل العبء الحسابي على محرك SQLite.

## استعلامات الإحصائيات (getDashboardStats)

تعتمد لوحة القيادة على مجموعة من الاستعلامات التجميعية (Aggregations). تعتمد هذه الاستعلامات على دوال مثل `SUM` و `COUNT`، ولذلك لم تتأثر بمشكلة `SELECT *`. 

أظهرت خطة التنفيذ استخداماً مباشراً لفهارس التصفية الأساسية. ومع ذلك، تم ملاحظة أن التصفية باستخدام الدالة `date(created_at)` تمنع الاستفادة القصوى من فهرس التاريخ. كخطوة مستقبلية، يوصى باستخدام مقارنات النطاق الزمني المباشرة (`created_at >= ? AND created_at < ?`).

## التحقق من مسارات التنفيذ (Integration)

تم فحص الواجهات المستهلكة مثل `sales-log.html` و `dashboard.html` لضمان توافقها مع التعديلات. تعتمد واجهة المبيعات بشكل مباشر على الحقول التي تم تضمينها في الإسقاط الصريح (مثل `sale_id` و `customer_name` و `net_amount`)، مما يؤكد عدم وجود أي فقدان للبيانات. جميع المدخلات الديناميكية تمر عبر مصفوفات معلمات (`args.toTypedArray()`) لضمان الحماية ضد ثغرات حقن SQL.

## 7. فحص الاستعلامات الأعلى تكلفة المتبقية
- **قائمة الأطراف (getParties)**: تم استبدال `SELECT p.*` بإسقاط صريح يحدد جميع حقول الجدول (حوالي 45 حقلاً) بشكل مفصل. يمنع هذا التعديل أي كسر في الواجهات عند إضافة أعمدة جديدة للمخطط مستقبلاً دون تحديث الجسر.
- **سجل الرسائل (getSmsMessagesPage)**: تم استبدال `SELECT sm.*` بإسقاط صريح لجميع حقول الرسالة بالإضافة إلى حقول فرعية مثل `owner_name` و `cognitive_state`.
- **سجلات النظام والتدقيق والوثائق**: تم فحص `getSystemLogsPageTyped`، `getAuditLogsPageTyped`، و `getDocumentsPageTyped`، ووجد أنها تستخدم إسقاطات صريحة بالفعل (مثل `SELECT id, uuid, log_level, log_type...`) ولا تستخدم `SELECT *`.

## الخلاصة
تمت معالجة جميع الاستعلامات المرتبطة بالتقارير والمبيعات والمخزون والعملاء وسجلات النظام لإزالة `SELECT *` والاعتماد على الإسقاط الصريح، مع تحسين استعلامات `COUNT` المرافقة للترقيم. تم فحص خطط التنفيذ (EXPLAIN QUERY PLAN) لضمان استخدام الفهارس بشكل صحيح وتجنب الانضمامات غير الضرورية. تم التحقق من سلامة البناء (Build) وعدم كسر العقود (Contracts) بين قاعدة البيانات والجسر وواجهة المستخدم.
